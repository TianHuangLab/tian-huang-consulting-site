# Vendor Integration

This folder contains the local Astro integration used to load `src/config.yaml` and expose site configuration to Astro components.

It is part of the project runtime and should be kept unless the configuration system is replaced.
